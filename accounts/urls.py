from django.urls import path

from . import views

urlpatterns= [
    # path('signin', views.SignupView.as_view(), name='signupview'),
    # path('login', views.SigninView.as_view(), name='signinview'),
    # path('logout', views.LogoutView.as_view(), name='logoutview'),
]